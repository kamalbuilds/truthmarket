{
  "name": "truthmarket",
  "version": "1.0.0",
  "description": "Manipulation-proof prediction markets using GenLayer AI consensus",
  "private": true,
  "scripts": {
    "deploy": "genlayer deploy",
    "deploy:bradbury": "genlayer network testnet-bradbury && genlayer deploy",
    "studio": "genlayer studio",
    "test": "bun run test/e2e-test.mjs",
    "build": "cd frontend && bun run build",
    "dev": "cd frontend && bun run dev"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "genlayer-js": "^0.23.0"
  }
}
